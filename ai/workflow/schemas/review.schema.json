{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-agent-workflow-runtime.local/schemas/review-v1.json",
  "title": "Workflow Review Artifact",
  "type": "object",
  "required": ["schemaVersion", "runId", "reviewer", "status", "findings"],
  "properties": {
    "schemaVersion": {"const": 1},
    "runId": {"type": "string", "minLength": 1},
    "reviewer": {"type": "string", "minLength": 1},
    "status": {"enum": ["pass", "fail", "blocked"]},
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "severity", "summary", "uncertainty", "confidence", "evidence", "recommendation", "resolved"],
        "properties": {
          "id": {"type": "string", "minLength": 1},
          "severity": {"enum": ["critical", "high", "medium", "low", "info"]},
          "summary": {"type": "string", "minLength": 3},
          "uncertainty": {"enum": ["confirmed", "likely", "possible", "unknown"]},
          "confidence": {"type": "number", "minimum": 0, "maximum": 1},
          "evidence": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": ["source", "detail"],
              "properties": {
                "source": {"type": "string", "minLength": 1},
                "line": {"type": ["integer", "null"]},
                "detail": {"type": "string", "minLength": 3}
              }
            }
          },
          "recommendation": {"type": "string", "minLength": 3},
          "file": {"type": ["string", "null"]},
          "resolved": {"type": "boolean"},
          "tags": {"type": "array", "items": {"type": "string"}},
          "conflictsWith": {"type": "array", "items": {"type": "string"}}
        }
      }
    }
  }
}
